BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[resumes] ADD [pageColor] NVARCHAR(1000) NOT NULL CONSTRAINT [resumes_pageColor_df] DEFAULT 'WHITE';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
