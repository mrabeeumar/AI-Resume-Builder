BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[resume_sections] ADD [hidden] BIT NOT NULL CONSTRAINT [resume_sections_hidden_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
